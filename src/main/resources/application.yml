spring:
  main:
    banner-mode: "off"
  output:
    ansi:
      enabled: always

  profiles:
    #active: dev
    active: prod

  jpa:
    show-sql: true
    hibernate:
      format_sql: true
      ddl-auto: update