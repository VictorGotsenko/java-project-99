spring:
#profiles: production
  datasource:
    driver-class-name: org.postgresql.Driver
#переменная ${JDBC_DATABASE_URL}
#jdbc:postgresql://dpg-d34jj27fte5s73cl8350-a:5432/pagevalidarordb?password=f1CZp6XrqaLGTE5yj0ENt8jtQqPj10dy&user=pagevalidarordb_user
    #url: ${DB_URL:jdbc:postgresql://localhost:5432/myapp}
    #username: ${DB_USERNAME:postgres}
    #password: ${DB_PASSWORD:password}
    #url: jdbc:postgresql://localhost:5432/project
    #username: db_user
    #password:
    #url: ${JDBC_DATABASE_URL}
    #username: ${USERNAME}
    #password: ${PASSWORD}
    #// for deploy
    url: jdbc:postgresql://dpg-d3psjcer433s73am7q8g-a:5432/prj99
    username: ${USERNAME}
    password: ${PASSWORD}

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
