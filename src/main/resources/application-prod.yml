spring:
#profiles: production
  datasource:
    driver-class-name: org.postgresql.Driver
    #url: jdbc:postgresql://localhost:5432/project
    #username: db_user
    #password:
    url: ${JDBC_DATABASE_URL}
    #username: ${USERNAME}
    #password: ${PASSWORD}
    #// for deploy
    #url: jdbc:postgresql://dpg-d3psjcer433s73am7q8g-a:5432/prj99
    #username: ${USERNAME}
    #password: ${PASSWORD}

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
